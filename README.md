
  # Onboarding Flow Prototype Review

  This is a code bundle for Onboarding Flow Prototype Review. The original project is available at https://www.figma.com/design/cb6By7SMWXCnkZQYQBTwqu/Onboarding-Flow-Prototype-Review.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  